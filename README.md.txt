# Math Adventures — Adaptive Learning Prototype

Run:
1. Create and activate venv
2. pip install -r requirements.txt
3. streamlit run src/main.py

This Streamlit app generates arithmetic puzzles, tracks correctness and time, and adapts difficulty (Easy/Medium/Hard) using a simple sliding-window, rule-based engine.
